export default class {

  constructor() {
    // current user, undefined if not yet logged in
    this.currentUser = currentUser;

    // likes of this user, undefined if not yet logged in
    this.likes = likes;

    // read only is false to show a button to log in
    this.readOnly = readOnly;

  }

}