import { h, render, Component } from 'preact';

/**
 * Render the list of talk feedback
 */
export default class TalkFeedbackListView extends Component {

  constructor({ user }) {
    super();
  }

  render() {
    //const html = `<ul className="ka-entries"></ul>`
    return (
      <div className="ka-feedback-entries">TODO: TalkFeedbackListView.render()</div>
    )
  }

}